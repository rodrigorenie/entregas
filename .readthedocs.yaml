version: 2

build:
  image: latest

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - docx
        - nltk
    - method: setuptools
      path: another/package
  system_packages: true